ADD:
- grind tactic
- metavariables
- try tactic
- ite typeclass
    - `if then else` notation
- `rewrite` tactic (before `rw`)
- generalize tactic
- simp at h ⊢
    turnstyle identifies the goal
- partial def